enum Status { active, inactive, completed }

enum TextFieldType { email, password, text, phoneNumber, otp, amount, notes }

enum AuthStatus { initial, authenticated, unauthenticated }

enum ToastVariants {success, error}

enum Roles {admin, employee}

enum InfoCardType {amount, number}

enum ExpenseStatus { pending, approved, rejected }
